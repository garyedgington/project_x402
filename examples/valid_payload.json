{
  "json_schema": {
    "type": "object",
    "required": ["name", "age"],
    "properties": {
      "name": {"type": "string"},
      "age": {"type": "integer", "minimum": 0},
      "role": {"type": "string", "enum": ["admin", "user"]}
    },
    "additionalProperties": false
  },
  "payload": {
    "name": "Gary",
    "age": 42,
    "role": "admin"
  },
  "strictness": "normal",
  "repair": false,
  "explain": true
}
